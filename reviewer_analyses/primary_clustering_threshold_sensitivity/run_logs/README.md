# Execution logs

This directory contains the original Savont completion log for every
read-depth by primary-clustering-threshold grid cell. Large run directories and
FASTQ inputs are excluded; the scored grid is in
`../primary_clustering_threshold_sensitivity.tsv`.
