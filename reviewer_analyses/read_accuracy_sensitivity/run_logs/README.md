# Execution logs

`savont/` contains the original Savont completion log for every simulated
accuracy by read-depth cell. `badread/` contains the four complete Badread
simulation logs compressed losslessly with gzip; inspect them with `zless` or
`gzip -cd`. The simulation audit and scored grid are retained one directory up.
