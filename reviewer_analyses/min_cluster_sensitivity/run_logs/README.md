# Execution logs

This directory contains the original Savont completion log for every
read-depth by minimum-cluster-size grid cell. Large run directories and FASTQ
inputs are excluded; the scored grid is in `../min_cluster_sensitivity.tsv`.
